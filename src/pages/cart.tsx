import React, { FC } from 'react';
import CartSummary from '../components/CartSummary';

const Cart: FC = () => {
   
 return(
    <main>
          <CartSummary />
    </main>
 )
}

export default Cart;