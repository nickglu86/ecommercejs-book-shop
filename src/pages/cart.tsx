import React, { FC } from 'react';
import CartCheckout from '../components/CartCheckout';

const Cart: FC = () => {
   
 return(
    <main>
          <CartCheckout />
    </main>
 )
}

export default Cart;